# ── 凭据文件：永远不提交 ───────────────────────────────────────────────────
# skill 读取凭据时就认这几条路径，放进仓库等于把令牌公开。
.github-credentials
.git-secrets.local
github.env
.env
.env.*
!.env.example
secrets/
*.pem
*.key
*.pfx
*.p12
id_rsa*
id_ed25519*
*.token

# ── 发布流程运行时残留 ─────────────────────────────────────────────────────
# New-Capture 会把临时 .bat 与命令日志写到这里；每次发布结束后可整个删掉。
.dsh-release/

# ── 编辑器 / 操作系统 ─────────────────────────────────────────────────────
.vscode/
.idea/
*.swp
*~
.DS_Store
Thumbs.db
Desktop.ini

# ── 日志与临时产物 ─────────────────────────────────────────────────────────
*.log
*.tmp
*.bak
